# Copy to secrets.yaml and fill in. NEVER commit secrets.yaml.
api_encryption_key: "REPLACE_WITH_32_BYTE_BASE64_KEY"  # generate: esphome dashboard wizard, or: openssl rand -base64 32
ota_password: "REPLACE_WITH_OTA_PASSWORD"
wifi_ssid: "REPLACE_WITH_WIFI_SSID"
wifi_password: "REPLACE_WITH_WIFI_PASSWORD"
ap_password: "REPLACE_WITH_AP_FALLBACK_PASSWORD"
